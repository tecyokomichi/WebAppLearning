// Code generated by goa v3.5.2, DO NOT EDIT.
//
// sampleApi HTTP client CLI support package
//
// Command:
// $ goa gen sampleApi/design

package cli

import (
	"flag"
	"fmt"
	"net/http"
	"os"
	sampleapic "sampleApi/gen/http/sample_api/client"

	goahttp "goa.design/goa/v3/http"
	goa "goa.design/goa/v3/pkg"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//    command (subcommand1|subcommand2|...)
//
func UsageCommands() string {
	return `sample-api show
`
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + ` sample-api show --id 7306218495776817807` + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(
	scheme, host string,
	doer goahttp.Doer,
	enc func(*http.Request) goahttp.Encoder,
	dec func(*http.Response) goahttp.Decoder,
	restore bool,
) (goa.Endpoint, interface{}, error) {
	var (
		sampleAPIFlags = flag.NewFlagSet("sample-api", flag.ContinueOnError)

		sampleAPIShowFlags  = flag.NewFlagSet("show", flag.ExitOnError)
		sampleAPIShowIDFlag = sampleAPIShowFlags.String("id", "REQUIRED", "ID")
	)
	sampleAPIFlags.Usage = sampleAPIUsage
	sampleAPIShowFlags.Usage = sampleAPIShowUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "sample-api":
			svcf = sampleAPIFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "sample-api":
			switch epn {
			case "show":
				epf = sampleAPIShowFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     interface{}
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "sample-api":
			c := sampleapic.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "show":
				endpoint = c.Show()
				data, err = sampleapic.BuildShowPayload(*sampleAPIShowIDFlag)
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// sample-apiUsage displays the usage of the sample-api command and its
// subcommands.
func sampleAPIUsage() {
	fmt.Fprintf(os.Stderr, `Sample API Service
Usage:
    %[1]s [globalflags] sample-api COMMAND [flags]

COMMAND:
    show: Show implements show.

Additional help:
    %[1]s sample-api COMMAND --help
`, os.Args[0])
}
func sampleAPIShowUsage() {
	fmt.Fprintf(os.Stderr, `%[1]s [flags] sample-api show -id INT

Show implements show.
    -id INT: ID

Example:
    %[1]s sample-api show --id 7306218495776817807
`, os.Args[0])
}
